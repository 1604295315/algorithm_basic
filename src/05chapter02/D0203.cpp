// AcWing 154 滑动窗口
// 单调队列优化
// 滑动窗口里的最大值与最小值
// 单调队列

// n k 1e6
// 单调队列 最小值为队头?
#include <stdio.h>

const int N = 1000010;

int q[N], tt, hh;

int n;

int main() {
    scanf("%d", &n);
    for (int i = 0; i < n; i ++)
        scanf("%d", &q[i]);
        
    int hh = 0; tt = -1;
    for (int i = 0; i < n; i ++) {
        
    }
    return 0;
}
